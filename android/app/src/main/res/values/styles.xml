<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#000000</item>
    </style>
    <style name="AppThemea" parent="Theme.AppCompat.Light.DarkActionBar"></style>

    <style name="AppThemed" parent="Theme.AppCompat.Light.DarkActionBar"></style>

    <style name="AppThemea.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="AppThemea.NoActionBar.Fullscreen">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppThemea.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppThemea.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>